<?xml version="1.0" encoding="utf-8"?>
<!--
  The Wispr Flow arrangement: one bar of our own above a keyboard that is otherwise the platform's.
  Menu on the left, a filled Start pill on the right, and the status line only speaks when it has
  something to say.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/kb_bg"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="68dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="14dp"
        android:paddingEnd="6dp">

        <ImageButton
            android:id="@+id/btn_menu"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_panel_key"
            android:contentDescription="@string/cd_menu"
            android:padding="8dp"
            android:src="@drawable/ic_menu"
            android:tint="@color/kb_label" />

        <TextView
            android:id="@+id/status"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:fontFamily="sans-serif"
            android:maxLines="2"
            android:text="@string/status_idle"
            android:textColor="@color/kb_muted"
            android:textSize="13sp" />

        <ImageButton
            android:id="@+id/btn_cancel"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="4dp"
            android:background="@drawable/bg_panel_key"
            android:contentDescription="@string/cd_cancel"
            android:padding="9dp"
            android:src="@drawable/ic_close"
            android:tint="@color/kb_muted"
            android:visibility="invisible" />

        <com.zaynikhlaq.dodostt.StartPillView
            android:id="@+id/pill"
            android:layout_width="106dp"
            android:layout_height="44dp"
            android:contentDescription="@string/cd_mic" />
    </LinearLayout>

    <com.zaynikhlaq.dodostt.KeyboardView
        android:id="@+id/keyboard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="6dp" />
</LinearLayout>
